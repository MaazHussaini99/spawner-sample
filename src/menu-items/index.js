import sidebarMenus from './menu-items';
import logout from './logout';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [sidebarMenus, logout]
};

export default menuItems;
